# SWE.5 Integration sequence and checks

Generate the preflight and build envelope, then execute parser self-tests, integrated probing against the isolated prefix, and requirement-based validation. No runtime source exists in this slice. Preserve exact package binaries outside Git and record their hashes and retrieval coordinates for offline reconstruction.
