# SWE.5 Integration sequence and checks

Use one Luna evidence worker on the immutable reviewed candidate, require an exact two-file evidence-only diff, run all five host measures on the resulting candidate, then require a separate Astra review.
