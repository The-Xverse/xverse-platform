# Design and verification plan

Reworks the rejected provider candidate without adding capability scope. LifecycleController gains minimal read-only declaration accessors that return owned copies only after exact handle authentication, allowing ProviderComposition to compare the caller-supplied RouteSpec and endpoint declarations against the authoritative retained values before provider preparation. Provider implementations return bounded internal operation results; ProviderComposition alone issues public ProviderRouteHandle and ProviderRouteSnapshot values, so an independent conforming provider can participate without minting composition authority. Registration captures and validates descriptor and instance identity before acquiring the registry lock, then stores the captured values without any virtual call under that lock. The agreed sequence is prepare provider while the lifecycle route is validated, activate the provider resource, activate the lifecycle route, then allow submit. Composition performs exact lifecycle and item validation before dispatch, while the provider retains defensive validation. Prior validators gain only an explicit additive-owned-path input; their substantive checks execute unchanged.

- XCOM-PROVIDER-CONTRACT-REPAIR: Repair F1 through F4 in C++ only. Add exact authenticated retained-declaration access, redesign provider internal results so independent implementations work while composition alone issues public handles and snapshots, remove every provider virtual call from composition lock scopes, enforce composition-side exact lifecycle and item checks, and implement the agreed prepare-provider then activate-provider then activate-lifecycle sequence. Preserve all existing behavior and add no new capability.; checks: VM-XCOM-PROV-LINT
- XCOM-PROVIDER-VERIFICATION-REPAIR: Repair F5 and F6 after the contract task. Add independent-provider and re-entry fixtures; complete route, endpoint, contract, provider, digest, generation, item contract/schema/version/kind/provenance, and no-mutation negative matrices; run overlapping producers and consumers and verify unique delivered content. Replace prior-validator monkeypatching with an explicit minimal additive-owned-path interface while executing every substantive accepted gate unchanged. Complete the provider validator and all five declared measures.; checks: VM-XCOM-PROV-UNIT, VM-XCOM-PROV-LINT, VM-XCOM-PROV-STATIC, VM-XCOM-PROV-INTEGRATION, VM-XCOM-PROV-VALIDATION
- XCOM-PROVIDER-DOCUMENTATION-REPAIR: Reconcile F4 and all F1-F6 dispositions in provider documentation and reciprocal traceability after code and verification repairs. Describe the exact preparation/activation/submission sequence, validation ownership, independent provider extension mechanism, lock boundaries, regression integration, maturity, and evidence.; checks: VM-XCOM-PROV-STATIC
