# SWE.3 Detailed design and units

Add authenticated LifecycleController accessors for EndpointSpec and RouteSpec owned copies without changing existing handle or transition behavior. During preparation, ProviderComposition retrieves and compares complete retained route/source/destination declarations, including contract, provider, plan digest, identities, and directions. Redesign CommunicationProvider so any conforming implementation can return bounded internal token and state values with public validating factories or interface-owned result types; only ProviderComposition may wrap them in public authority-bearing handles and snapshots. Capture descriptor and instance identity before taking the composition mutex and never invoke a provider virtual method while that mutex is held. Composition validates exact lifecycle state and every CommunicationItem contract, schema, version, interaction, source, route, provider, and payload binding before provider dispatch. Adopt prepare-provider, activate-provider, activate-lifecycle, submit as the authoritative sequence. Add a separately implemented minimal provider syntax/runtime fixture, a re-entrant registration probe, complete preparation/submission mismatch matrices with no-mutation assertions, and truly overlapping concurrent producers/consumers with unique item verification. Replace validator monkeypatches with a small explicit additive-owned-path argument in prior validators while retaining and exercising their complete original gate bodies.

- XCOM-PROVIDER-CONTRACT-REPAIR: Repair F1 through F4 in C++ only. Add exact authenticated retained-declaration access, redesign provider internal results so independent implementations work while composition alone issues public handles and snapshots, remove every provider virtual call from composition lock scopes, enforce composition-side exact lifecycle and item checks, and implement the agreed prepare-provider then activate-provider then activate-lifecycle sequence. Preserve all existing behavior and add no new capability.; owned paths: src/xverse/xcom
- XCOM-PROVIDER-VERIFICATION-REPAIR: Repair F5 and F6 after the contract task. Add independent-provider and re-entry fixtures; complete route, endpoint, contract, provider, digest, generation, item contract/schema/version/kind/provenance, and no-mutation negative matrices; run overlapping producers and consumers and verify unique delivered content. Replace prior-validator monkeypatching with an explicit minimal additive-owned-path interface while executing every substantive accepted gate unchanged. Complete the provider validator and all five declared measures.; owned paths: tests/xcom/provider_loopback, scripts/validate_xcom_provider_loopback.py, scripts/validate_xcom_core_types.py, scripts/validate_xcom_endpoint_route_lifecycle.py
- XCOM-PROVIDER-DOCUMENTATION-REPAIR: Reconcile F4 and all F1-F6 dispositions in provider documentation and reciprocal traceability after code and verification repairs. Describe the exact preparation/activation/submission sequence, validation ownership, independent provider extension mechanism, lock boundaries, regression integration, maturity, and evidence.; owned paths: docs/xcom/provider-composition-loopback.md, docs/xcom/provider-composition-loopback-traceability.json
