# SWE.5 Integration sequence and checks

Generate the complete slice through one isolated SESN task against the accepted b0a8c25 baseline. Build a separate public-API consumer that creates contracts and items, declares and activates lifecycle resources, registers the loopback provider explicitly, prepares and activates routes, exchanges signal/message/request/response items, fills and recovers a queue, drains, closes, reconciles, and recreates resources. Run table-driven negative and concurrency tests plus source/dependency/isolation scans. The complete validation measure reruns the accepted core-types and endpoint/route lifecycle validators unchanged. Bind every evidence item to the immutable candidate revision, generate warning-free Doxygen, and run a separate Astra review before READY_FOR_REVIEW.
