# SWE.5 Integration sequence and checks

Use the already admitted offline C++20 build envelope and add one library target, unit/negative CTest targets, and a separate external-consumer fixture. A bounded Python validation driver configures disposable Ninja builds, runs warning-as-error compilation, CTest, the admitted clang-tidy, warning-free Doxygen, forbidden-I/O scans, and traceability checks. Every SESN measure runs with network disabled and binds evidence to the candidate revision.
