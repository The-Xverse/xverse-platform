# SWE.5 Integration sequence and checks

Use one Lua/Luna evidence worker on the immutable failed candidate, verify its diff contains exactly one semantic list addition, run all five host measures on the resulting immutable candidate, then require an independent Astra review.
