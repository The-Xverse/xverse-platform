# SWE.5 Integration sequence and checks

Update only build-environment.md, then run self-test, Ruff, strict mypy, isolated toolchain integration, and all-requirement validation. Inspect every evidence manifest for allow_network=false and bubblewrap-unshare-net before independent review.
