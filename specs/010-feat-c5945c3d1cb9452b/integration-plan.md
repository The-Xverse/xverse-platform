# SWE.5 Integration sequence and checks

Run parser/static checks after each unit. Exercise negative fixtures for policy-negating compiler flags, changes anywhere in the complete locked package payload, provenance rejection before executable probing, filesystem/Unicode/subprocess/temp-directory failures, and each required document independently. Then execute the isolated-prefix integration and all-requirement validation checks, preserve host evidence, and obtain an independent read-only review.
