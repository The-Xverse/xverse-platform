# SWE.5 Integration sequence and checks

Run three dependency-ordered SESN workers against rejected immutable candidate 0c0c82457d111a33828d9d6c168875e8caf17611. First close the provider authority boundary in C++ only. Second add isolated counterexamples and restore the complete prior gates. Third reconcile only the two provider evidence documents using a lower-cost evidence profile. Integrate clean task commits, run all five host measures on one immutable candidate, then require a fresh Astra review.
