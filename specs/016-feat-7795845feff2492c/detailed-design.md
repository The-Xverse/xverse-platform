# SWE.3 Detailed design and units

Move prepare, activate, submit, receive, drain, close, snapshot, and reconciliation mutations behind private CommunicationProvider virtual entry points and friend only ProviderComposition. Independent implementations override those private virtuals without needing to construct public handles. Remove public raw-token mutation paths from LoopbackProvider. Add a separate external consumer compile-rejection probe. Use fresh unprepared scenarios with dispatch counters for every preparation mismatch so duplicate-route rejection cannot mask missing composition checks. Add stale, failed, closed, recreated, and route-capacity activation cases, exact diagnostic-byte assertions, and before/after no-mutation snapshots. Restore recursive prior production enumeration and strict Doxygen inputs; additive owned paths only extend ownership admission. Add provider.cpp to submission and cleanup requirement/unit edges in both traceability directions. Correct the sequence to show handle issuance during preparation and composition-owned lifecycle activation. Describe intermediate commits as task evidence and bind delivery only to SESN's final candidate.

- XCOM-PROVIDER-AUTHORITY-REPAIR: Repair R1 in C++ only. Make every state-changing CommunicationProvider and LoopbackProvider operation inaccessible to external consumers and callable only by ProviderComposition, while keeping independent provider overrides possible and public read-only descriptor access intact. Preserve bounded behavior, lock rules, and the implemented composition-owned activation sequence.; owned paths: src/xverse/xcom/include/xverse/xcom/provider.hpp, src/xverse/xcom/include/xverse/xcom/loopback_provider.hpp, src/xverse/xcom/src/provider.cpp, src/xverse/xcom/src/loopback_provider.cpp
- XCOM-PROVIDER-VERIFICATION-GATE-REPAIR: Repair R2 and R3 after the authority task. Add an external-consumer compile rejection probe, fresh unprepared dispatch-counted mismatch fixtures, the stale/failed/closed/recreated and route-capacity activation matrix, exact diagnostic-byte assertions, and before/after no-mutation checks. Restore every substantive prior recursive source, documentation, ownership, lifetime, thread-safety, failure-clause, build, and runtime gate; additive path admission may affect ownership only.; owned paths: tests/xcom/provider_loopback, scripts/validate_xcom_provider_loopback.py, scripts/validate_xcom_core_types.py, scripts/validate_xcom_endpoint_route_lifecycle.py
- XCOM-PROVIDER-EVIDENCE-REPAIR: Repair R4 through R6 in the two provider evidence documents. Add reciprocal provider.cpp submission and cleanup allocations, document handle issuance during preparation and composition-owned lifecycle activation, and remove any claim that an intermediate task revision is the final candidate. Bind delivery evidence to the host-identified immutable candidate and preserve maturity.; owned paths: docs/xcom/provider-composition-loopback.md, docs/xcom/provider-composition-loopback-traceability.json
