# SWE.3 Detailed design and units

Preserve immutable EndpointSpec and RouteSpec values, opaque EndpointHandle and RouteHandle values, LifecycleState snapshots, and the serialized fixed-capacity LifecycleController. Repair Diagnostic ordering-key storage by deriving a compile-time worst-case escaped capacity from the longest stable lifecycle phase/code plus separators and maximum identity/reason/correction lengths, and make append operations bounds checked even if a future enum grows. Add maximum-length pipe-escaping regression tests. Keep RouteSpec responsible only for declaration compatibility; LifecycleController validates exact route and endpoint handles, generations, digests, ownership, endpoint state, and endpoint-use constraints. Add independent negative cases for every handle and endpoint boundary at validation and activation, always query fresh snapshots after rejection, and test closure while routes retain endpoints. Make the traceability validator compare each document allocation against authoritative measure requirement IDs in both directions. Extend the external consumer through safe repeats, skipped-transition rejection, closure, and recreation of both endpoints and the route. Preserve all existing accepted core-types behavior and every original exclusion.

- XCOM-ENDPOINT-ROUTE-LIFECYCLE: Repair all six findings from REVIEW-5062aa21bd884561 in the existing rejected lifecycle candidate. XCOM-REV-001: derive and guard diagnostic ordering-key capacity and add maximum escaping tests. XCOM-REV-002: add independent validation/activation/retained-endpoint/stale/foreign tests with exact diagnostics and fresh no-mutation snapshots. XCOM-REV-003: reconcile traceability allocations and enforce authoritative reciprocal equality. XCOM-REV-004: complete the external consumer with safe repeat, invalid transition, closure, and recreation of both endpoints and route. Preserve the regenerated XCOM-REV-005 unit-responsibility split and XCOM-REV-006 EARS correction. Rerun all five measures and the accepted core-types regression. Do not weaken a gate or add provider, transport, queue, item flow, XDL compilation, observation, stimulation, gateway, dashboard, or legacy behavior.; owned paths: src/xverse/xcom, tests/xcom/endpoint_route_lifecycle, scripts/validate_xcom_endpoint_route_lifecycle.py, docs/xcom/endpoint-route-lifecycle.md, docs/xcom/endpoint-route-lifecycle-traceability.json
